import { Carousel } from 'antd';
import { CarouselRef } from 'antd/lib/carousel';
import React, { FC, useRef, useState } from 'react';
import styles from './styles.module.less';

interface Props{
  ref:CarouselRef;
}

const Slider: FC<Props> = ({ref}) => {
  return (
    <>
    
    </>
  );
};

export default Slider;
